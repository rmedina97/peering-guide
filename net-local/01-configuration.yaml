apiVersion: networking.liqo.io/v1beta1
kind: Configuration
metadata:
  creationTimestamp: null
  labels:   # Mandatory labels for Liqo
    liqo.io/remote-cluster-id: provider   # Replace with the actual remote cluster ID
  name: provider
  namespace: liqo-tenant-provider  
spec:
  remote:
    cidr:
      external:
      - 10.70.0.0/16    # Replace with the actual remote cluster CIDR
      pod:
      - 10.42.0.0/16    # Replace with the actual remote cluster pod CIDR
status: {}